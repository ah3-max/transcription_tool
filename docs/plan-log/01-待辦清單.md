# 待辦清單（needed tasks）— stt-translate

> 建立：2026-06-26 ｜ 類型：可勾選行動清單（搭配 `00-接手總覽-現況與四軌路線圖.md` 看全局）。
> **本檔狀態最新**：較 `00-接手總覽` 新（S-10/S-12 已落地、S-06 進行中）；要我同步 00 的狀態表再說一聲。

---

## 🔵 現在進行：S-06 即時 WebSocket 串流（主攻）
> plan：`Sprint1-S06-ws-streaming.md`。**動手前必讀該檔 §2.5 PoC 接線清單**（PoC 已踩過的雷）。
- [ ] `services/asr.py` 接 `/v1/realtime`：握手要 `commit(final=False)` 才啟動、剝 `language {lang}<asr_text>` 前綴
- [ ] `ws/live.py` → `/ws/live`：音框 → VAD → ASR → 即時翻譯 → 下行 `{type:partial|final, src, translations, t}`（`t` 用伺服器時鐘）
- [ ] VAD／重疊切段（避免固定 5s 在段界切字）
- [ ] **第 5 步：凍結 `sessions`/`outputs` 寫入契約** ← 先做，解 S-07 後端依賴
- [ ] SEC-6 加固：限同網段來源、限連線數/訊息大小、閒置斷線
- [ ] 前端即時頁接線：interim 半透明／final 實心
- [ ] 消費 S-10 的 `degraded` 下行（同時完成 S-10 的端到端驗）

## 🧰 Host 端前置（擋住 S-06 翻譯半截／S-12 第三臂）
- [ ] **NLLB 即時翻譯服務 :8001** — 步驟見 `00-接手總覽` §3；建 `~/.venvs/nllb` 後**先驗 sm_120 kernel（cu130，比照 PoC）**；冒煙中→泰直譯；註冊 `function=live_tr`
- [ ] systemd unit `stt-vllm-asr`（以 realtime 架構啟動、可按需起停）— `services/idle.py:29` 已引用、待建

## 🟡 等人工收尾
- [ ] **S-12 中→泰評測**：素材／腳本／評分表已備（commit `9389f8e`）→ 跑批 → 母語者評分 → 彙整量化表 → 定 Phase 1 預設翻譯模型＋退路 → 補決策記錄/`.env` → 回填 dev_log（**交使用者**）
- [ ] **S-10 端到端**：降級橫幅程式路徑已過（pytest 綠）；待 S-06 WS 起後驗 `degraded` 下行端到端

## ⚠️ 半成品待補（PoC 已解鎖、app 端尚未接）
- [ ] **S-04 批次 ASR 接線**：`queued` job → 呼叫 ASR 端點轉錄 → worker 跑完 → 產出逐字稿＋真實進度（現只到 ffprobe 入庫，`routes/jobs.py:221` 進度寫死）
- [ ] **S-05 扇出翻譯串接**：`translate_fanout` 已就緒但無人呼叫 → 接上 S-04／錄音 pipeline
- [ ] **S-04 真前處理**：DeepFilterNet3 + Silero VAD + 每 15 分切段（`preprocess.py` 現只有 ffprobe 量時長）

## ⬜ 未開始
- [ ] **S-07 錄音記錄**：`routes/sessions.py`（API-07/08/09）＋前端記錄頁 — plan `Sprint2-S07-recordings.md`；S-06 第 5 步契約凍結後後端可並進，整合驗收須等 S-06
- [ ] **S-13 資安驗收**（末關）

## ✅ 已完成
- [x] S-01/02/03 骨架·內網綁定·儲存清除·模型路由與資源/閒置
- [x] S-08 文件生成、S-09 匯出/歷史、S-11 前端 v6 串接
- [x] S-10 降級與錯誤處理（`services/resources.py` live_readiness＋前端橫幅/toast＋pytest；e2e 待 S-06）
- [x] S-12 評測素材＋腳本（備料；評測本身見上「等人工收尾」）
- [x] D-16 vLLM-ASR PoC = GO（本 session 實機核對版本/sha/gcc/GPU）

## 🔀 版控待辦
- [ ] feat 分支累積 **14 個 commit 未合併**；`main` 落後 14、`origin/main` 停在 S-05（`68aaabf`）更舊 → 決定：合併 `feat/s06-ws-streaming` → `main` ＋推 `origin`（或開 PR）
- [ ] 清理重複分支：`feat/s08-s09-s11` 與現分支同點、內容已被涵蓋，可刪
- [ ] 合併前注意：`web/app.js` 多軌（B 全域橫幅／C 即時頁／D 記錄頁）改不同區塊，確認無交叉
